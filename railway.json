{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[STARTUP] Starting...' && npx prisma db push --accept-data-loss && echo '[STARTUP] Prisma complete, starting server...' && node dist/server.js 2>&1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

